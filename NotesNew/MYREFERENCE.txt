AntiGravity
/* Why Section */
.why-section, .stats-section {
  text-align: center;
  padding: 3rem 4rem;
  max-width: 1200px;
  margin: 0 auto;
}

.section-title, .faq-title {
  font-size: 2rem;
  color: var(--text-dark);
  position: relative;
  display: inline-block;
  margin-bottom: 3rem;
}

.section-title::after, .faq-title::after {
  content: '';
  position: absolute;
  bottom: -10px;
  left: 10%;
  width: 80%;
  height: 3px;
  background: var(--accent-teal);
  border-radius: 2px;
  transform: rotate(-1deg);
}

.cards-container {
  display: flex;
  gap: 2rem;
  justify-content: center;
}

.info-card {
  flex: 1;
  max-width: 500px;
  border-radius: 20px;
  background: linear-gradient(135deg, var(--card-bg-start), var(--card-bg-end));
  color: var(--white);
  text-align: left;
  padding: 3rem 2rem;
  position: relative;
  overflow: hidden;
  min-height: 400px;
}



ChatGPT


/* Why Section */
.why-section,
.stats-section {
  text-align: center;
  padding: 3rem 4rem;
  max-width: 1200px;
  margin: 0 auto;
}

.section-title,
.faq-title {
  font-size: 2rem;
  color: var(--text-dark);
  position: relative;
  display: inline-block;
  margin-bottom: 3rem;
}

.section-title::after,
.faq-title::after {
  content: '';
  position: absolute;
  bottom: -10px;
  left: 10%;
  width: 80%;
  height: 3px;
  background: var(--accent-teal);
  border-radius: 2px;
  transform: rotate(-1deg);
}


/* Cards */
.cards-container {
  display: flex;
  gap: 2rem;
  justify-content: center;
  align-items: stretch;
}


/* Card */
.info-card {
  flex: 1;
  max-width: 500px;
  height: 480px;

  border-radius: 20px;
  position: relative;
  overflow: hidden;

  color: var(--white);
  text-align: left;

  background: linear-gradient(
    135deg,
    var(--card-bg-start),
    var(--card-bg-end)
  );
}


/* Image container */
.info-card .ce-why-box-inner {
  width: 100%;
  height: 100%;
}


/* Image */
.info-card .ce-why-box-inner img {
  width: 100%;
  height: 100%;
  display: block;
  object-fit: cover;
  object-position: center;
}


/* Dark overlay */
.info-card::after {
  content: '';
  position: absolute;
  inset: 0;

  background: linear-gradient(
    to top,
    rgba(0, 0, 0, 0.75),
    rgba(0, 0, 0, 0.05)
  );

  z-index: 1;
}


/* Text */
.info-card .card-content {
  position: absolute;
  left: 35px;
  right: 35px;
  bottom: 35px;

  z-index: 2;
  color: white;
}


/* Number */
.info-card .card-content h3 {
  font-size: 3rem;
  margin: 0 0 10px;
  line-height: 1;
}


/* Description */
.info-card .card-content p {
  font-size: 1.1rem;
  line-height: 1.4;
  margin: 0 0 20px;
}


/* Badge */
.info-card .badge-pill {
  display: inline-block;
  padding: 8px 18px;

  border: 1px solid rgba(255, 255, 255, 0.7);
  border-radius: 30px;

  font-size: 0.9rem;
  color: white;
}


/* Right card image position */
.doctors-card .ce-why-box-inner img {
  object-position: center 25%;
}


/* Mobile */
@media (max-width: 767px) {

  .why-section,
  .stats-section {
    padding: 2rem 1rem;
  }

  .cards-container {
    flex-direction: column;
    gap: 1.5rem;
  }

  .info-card {
    width: 100%;
    max-width: 100%;
    height: 400px;
  }

  .info-card .card-content {
    left: 25px;
    right: 25px;
    bottom: 25px;
  }

  .info-card .card-content h3 {
    font-size: 2.2rem;
  }

  .info-card .card-content p {
    font-size: 1rem;
  }

  .info-card .badge-pill {
    font-size: 0.8rem;
  }
}